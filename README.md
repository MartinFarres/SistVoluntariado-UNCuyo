# Sistema voluntariado

## Configuración del proyecto


Creación del entorno virtual de python con *venv*
```bash
python -m venv .venv
```

Instalación de dependencias e inicialización del proyecto
```bash
chmod +x ./build.sh
./build.sh
```

Levantar el servidor
```bash
python manage.py runserver
```
